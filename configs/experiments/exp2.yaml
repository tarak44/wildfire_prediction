data:
  train_dir: data/train
  val_dir: data/valid
  test_dir:
  image_size: 224

train:
  batch_size: 16
  num_workers: 0
  epochs: 1
  lr: 0.001
  weight_decay: 0.00001
  device: cpu
  output_dir: artifacts/exp2
  seed: 42
  max_train_samples: 64
  max_val_samples: 32
  mlflow_tracking_uri: ./mlruns
  mlflow_experiment: wildfire-v1
  mlflow_run_name: exp2_lr1e-3
