data:
  train_dir: data/train
  val_dir: data/valid
  test_dir:
  image_size: 224

train:
  batch_size: 16
  num_workers: 0
  epochs: 1
  lr: 0.0003
  weight_decay: 0.00001
  device: cpu
  output_dir: artifacts/exp1
  seed: 42
  max_train_samples: 64
  max_val_samples: 32
  mlflow_tracking_uri: sqlite:///mlflow.db
  mlflow_experiment: wildfire-v1
  mlflow_run_name: exp1_lr3e-4
